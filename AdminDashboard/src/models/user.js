export default class User {
    constructor(username, password, email,keyzl) {
      this.username = username;
      this.password = password;
      this.email = email;
      this.keyzl = keyzl;

    }
  }